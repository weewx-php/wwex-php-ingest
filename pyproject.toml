[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "weewx-php-ingest"
version = "0.2.0"
description = "Durable, isolated WeeWX collectors for the weewx-php native receiver"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["weewx==5.5.0", "configobj>=5.0.9,<6", "six>=1.17"]

[project.optional-dependencies]
test = ["pytest>=8", "cryptography>=46", "ruff>=0.12", "pip-audit>=2.9", "build>=1.2", "requests>=2.32", "python-dateutil>=2.9", "pycryptodomex>=3.23", "six>=1.17"]

[project.scripts]
weewx-php-ingest = "weewx_php_ingest.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
extend-exclude = ["vendor", "tests/fixtures/*_upstream", "src/weewx_php_ingest/_vendor"]
target-version = "py311"
line-length = 100

[tool.setuptools.package-data]
"weewx_php_ingest._vendor" = ["*.json", "LICENSE*"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
